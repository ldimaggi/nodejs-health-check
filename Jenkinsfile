#!/usr/bin/groovy
@Library('github.com/hrishin/osio-pipeline@cd-fixes')

def STAGES = ['run', 'stage']
def templateParameters = ["SUFFIX_NAME": ""]

osio {
  templateConfig = templateParameters
  stages = STAGES
}
